<html lang="en">

<head>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="../CSS/style.css">
    <title>Para-ding</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css">
    <link rel="shortcut icon" href="download.png" type="image/x-icon">
    <style>
        * {
            font-family: -apple-system, BlinkMacSystemFont,
                'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell,
                'Open Sans', 'Helvetica Neue', sans-serif;
        }
    </style>
</head>

<body>

    <header>
        <!-- Nav bar header -->
        <nav>
            {{#if logged_in}}
            <a href="../user.handlebars">profile</a> |
            <button class="no-button" id="logout">logout</button>
            {{else}}
            <a href="../login.handlebars">login</a>
            {{/if}}
        </nav>
        <div class="header">
            <div class="header_left">
                <div class="search_box search">
                    <i class="bi bi-search"></i>
                    <input type="text" class="search_input" value="Search">
                </div>
            </div>
        </div>
        <div class="header_right">
            <div class="nav_link ">
                <div class="nav_icon">
                    <i class="bi bi-house-door-fill" style="font-size: 1.3rem  !important;"></i>
                </div>
                <div class="nav_text">Home</div>
                <a href="./main.handlebars"></a>
            </div>
            <div class="nav_link jobs">
                <div class="nav_icon"><i class="bi bi-bag-fill" style="font-size: 1.3rem  !important;"></i></div>
                <div class="nav_text">Jobs </a></a></div>
                <a href="../user.handlebars"></a>
            </div>
        </div>
        <div class="header_right_2" style="border-left: 1px solid grey;">
            <div class="nav_link">
                <div class="nav_icon">
                    <img src="../../public/imgs/R.png" alt="non" style="height:30px;object-fit: contain;border-radius:50px; ;">
                    <div class="nav_text dropdown drop">
                        <div class="dropdown">
                            <div id="myDropdown" class="dropdown-content">
                                <a href="../user.handlebars">Profile</a>
                                <a href="../searchjobs.handlebars">Home</a>
                            </div>
                        </div>
                        <i class="bi bi-caret-down-fill dropdown"
                            style="font-size: 1rem  !important; float: center;">
                        </i>
                        
                    </div>
                </div>
            </div>
        </div>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>

    </footer>
    {{#if logged_in}}
     <script src="../../public/JS/logout.js"></script>
     <script src="../../public/JS/search.js"></script>
    {{/if}}
</body>

</html>